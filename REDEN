import pyttsx3

def Baum(count):
    # Initialize the engine once outside the loop for better performance
    engine = pyttsx3.init()
    
    while count > 0:
        engine.say("Yassin")
        count -= 1
        engine.runAndWait()

Baum(500)
